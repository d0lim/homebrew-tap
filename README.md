# homebrew-tap

Homebrew tap for [ccswap](https://github.com/d0lim/ccswap).

```bash
brew install --cask d0lim/tap/ccswap
```

The cask in `Casks/` is generated by GoReleaser on each release. Do not edit it
by hand — the next release overwrites it.
